# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'NasaMapApplication' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SDWebImage', '~>3.7'
  # Pods for NasaMapApplication

  target 'NasaMapApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NasaMapApplicationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
